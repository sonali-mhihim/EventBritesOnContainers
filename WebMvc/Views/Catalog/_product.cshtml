@model WebMvc.Models.CatalogEvent




    <form asp-controller="Cart" asp-action="AddToCart">
        <img class="esh-catalog-thumbnail" src="@Model.PictureUrl" />
        <input class="esh-catalog-button" type="submit" value="[ ADD TO CART ]" />
        <div class="esh-catalog-name">
            <span>@Model.Name</span>
        </div>

        <div class="esh-catalog-price">
            <span>@(Model.Price==0 ? "FREE": "$ "+ Model.Price.ToString("N2"))</span>
        </div>
        <div class="esh-catalog-date">
            <span>@Model.Date.ToString("MM/dd/yyyy h:mm tt")</span>
        </div>
        <div class="esh-catalog-location">
            <span>@(Model.Location.Length > 30 ? (Model.Location.Substring(0,25)+"...") : Model.Location)</span>
        </div>
        <input type="hidden" asp-for="@Model.EventHost" name="host" />
        <input type="hidden" asp-for="@Model.CatalogHostId" name="hostId" />
        <input type="hidden" asp-for="@Model.EventType" name="type" />
        <input type="hidden" asp-for="@Model.CatalogTypeId" name="typeId" />
        <input type="hidden" asp-for="@Model.Description" name="description" />
        <input type="hidden" asp-for="@Model.Id" name="id" />
        <input type="hidden" asp-for="@Model.Name" name="name" />
        <input type="hidden" asp-for="@Model.PictureUrl" name="pictureUrl" />
        <input type="hidden" asp-for="@Model.Price" name="price" />
        <input type="hidden" asp-for="@Model.Date" name="date" />
        <input type="hidden" asp-for="@Model.Location" name="location" />

    </form>